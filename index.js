/**
 * Neural Bus - Unified Consciousness for AI Agents
 * 
 * @author Adam <adam@hsuite.ai>
 * @license MIT
 */

const { NeuralBusClient, connect, register, broadcast } = require('./client');

module.exports = {
  NeuralBusClient,
  connect,
  register,
  broadcast
};
